"""Bounded contexts (DDD) — um pacote por módulo de negócio, cada um com as quatro camadas de
Clean Architecture (`domain`/`application`/`infrastructure`/`interfaces`). Reconciliado para 25
pastas reais no Sprint 11, Lote 1.1 (`docs/backend/BACKEND_ARCHITECTURE.md`)."""
