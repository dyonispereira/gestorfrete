"""Camada de Domain do módulo integration. Contém as regras de negócio puras: entidades, value objects, eventos de domínio e as interfaces (ports) de repositório. Não depende de nenhuma outra camada (regra de dependência da Clean Architecture). Nesta etapa de fundação, apenas a estrutura existe — nenhuma entidade foi modelada ainda."""
