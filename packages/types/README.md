# @gestorfrete/types

Tipos TypeScript compartilhados entre aplicações do monorepo. O plano de longo prazo é gerar estes
tipos automaticamente a partir do schema OpenAPI exposto pela API (`apps/api`), garantindo que o
contrato entre frontend e backend nunca fique dessincronizado — isso será conectado quando a API
tiver seus primeiros endpoints reais.
