# @gestorfrete/config

Configurações compartilhadas de ferramentas de desenvolvimento entre todas as aplicações e pacotes
TypeScript do monorepo: ESLint, TypeScript (`tsconfig` base) e Tailwind. Centralizar aqui evita que
cada app do monorepo divirja de padrão de lint/estilo conforme o número de aplicações cresce.
